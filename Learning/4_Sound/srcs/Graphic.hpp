// #pragma once

// #include <vector>
// #include <string>

// class Graphics {
// public:
//     Graphics(int width, int height);
//     ~Graphics();

//     void run(const std::vector<std::vector<int>>& map);

// private:
//     void handleEvents();
//     void render(const std::vector<std::vector<int>>& map);
//     bool loadTilemap(const std::string& path);

// private:
//     int windowWidth;
//     int windowHeight;

//     bool running = true;


//     struct SDL_Window* window = nullptr;
//     struct SDL_Renderer* renderer = nullptr;
//     struct SDL_Texture* tileTexture = nullptr;

// };